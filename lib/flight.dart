import 'package:flutter/material.dart';

class FlightPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Flight')),
      body: Center(child: Text('Flight Page')),
    );
  }
}
